// Code generated by stego. DO NOT EDIT.

// Code generated by STEGO. DO NOT EDIT.
package storage

import (
	"context"
	"errors"
	statecontract "github.com/example/service/out/contracts/storage"
	"gorm.io/gorm"
	"math"
)

const ResourceStateScopesMigration = "SET LOCAL lock_timeout='5000';\nSET LOCAL statement_timeout='25000';\nCREATE TABLE IF NOT EXISTS stego_resource_state_scopes (\n entity text COLLATE \"C\" NOT NULL, scope text COLLATE \"C\" NOT NULL,\n revision bigint NOT NULL, sealed boolean NOT NULL,\n PRIMARY KEY(entity,scope),\n CHECK(octet_length(entity) BETWEEN 1 AND 256),\n CHECK(octet_length(scope) BETWEEN 1 AND 128), CHECK(revision>=0), CHECK(NOT sealed OR revision>0)\n);\nLOCK TABLE stego_resource_state IN SHARE ROW EXCLUSIVE MODE;\nINSERT INTO stego_resource_state_scopes(entity,scope,revision,sealed) SELECT entity,scope,count(*),false FROM stego_resource_state GROUP BY entity,scope ON CONFLICT(entity,scope) DO NOTHING;\nCREATE OR REPLACE FUNCTION stego_guard_resource_state_scope() RETURNS trigger LANGUAGE plpgsql SET search_path=pg_catalog AS $guard$\nBEGIN\n IF TG_OP = 'DELETE' THEN\n  RAISE EXCEPTION 'resource state scope history cannot be removed' USING ERRCODE='23514';\n END IF;\n IF NEW.entity IS DISTINCT FROM OLD.entity OR NEW.scope IS DISTINCT FROM OLD.scope\n    OR OLD.sealed OR OLD.revision=9223372036854775807 OR NEW.revision IS DISTINCT FROM OLD.revision+1 THEN\n  RAISE EXCEPTION 'resource state scope is closed or its revision differs' USING ERRCODE='23514';\n END IF;\n RETURN NEW;\nEND;\n$guard$;\nDROP TRIGGER IF EXISTS stego_resource_state_scope_guard ON stego_resource_state_scopes;\nCREATE TRIGGER stego_resource_state_scope_guard BEFORE UPDATE OR DELETE ON stego_resource_state_scopes FOR EACH ROW EXECUTE FUNCTION stego_guard_resource_state_scope();\nCREATE OR REPLACE FUNCTION stego_track_resource_state_keys() RETURNS trigger LANGUAGE plpgsql SET search_path=pg_catalog AS $guard$\nDECLARE current_revision bigint; current_sealed boolean; existing_key boolean;\nBEGIN\n EXECUTE format('INSERT INTO %I.stego_resource_state_scopes(entity,scope,revision,sealed) VALUES($1,$2,0,false) ON CONFLICT(entity,scope) DO NOTHING',TG_TABLE_SCHEMA)\n  USING NEW.entity,NEW.scope;\n EXECUTE format('SELECT revision,sealed FROM %I.stego_resource_state_scopes WHERE entity=$1 AND scope=$2 FOR UPDATE',TG_TABLE_SCHEMA)\n  INTO STRICT current_revision,current_sealed USING NEW.entity,NEW.scope;\n EXECUTE format('SELECT EXISTS(SELECT 1 FROM %I.stego_resource_state WHERE entity=$1 AND resource_id=$2 AND scope=$3)',TG_TABLE_SCHEMA)\n  INTO existing_key USING NEW.entity,NEW.resource_id,NEW.scope;\n IF existing_key THEN RETURN NEW; END IF;\n IF current_sealed THEN\n  RAISE EXCEPTION 'resource state scope is closed' USING ERRCODE='23514',CONSTRAINT='stego_resource_state_scope_closed';\n END IF;\n IF current_revision=9223372036854775807 THEN\n  RAISE EXCEPTION 'resource state scope revision is exhausted' USING ERRCODE='23514';\n END IF;\n EXECUTE format('UPDATE %I.stego_resource_state_scopes SET revision=revision+1 WHERE entity=$1 AND scope=$2',TG_TABLE_SCHEMA)\n  USING NEW.entity,NEW.scope;\n RETURN NEW;\nEND;\n$guard$;\nDROP TRIGGER IF EXISTS stego_resource_state_key_guard ON stego_resource_state;\nCREATE TRIGGER stego_resource_state_key_guard BEFORE INSERT ON stego_resource_state FOR EACH ROW EXECUTE FUNCTION stego_track_resource_state_keys();"

var _ statecontract.ResourceStateScopeStore = (*Store)(nil)

func init() {
	Register("011_resource_state_scopes", func(db *gorm.DB) error {
		return db.Transaction(func(tx *gorm.DB) error {
			for _, statement := range []string{"SET LOCAL lock_timeout='5000'", "SET LOCAL statement_timeout='25000'", "CREATE TABLE IF NOT EXISTS stego_resource_state_scopes (\n entity text COLLATE \"C\" NOT NULL, scope text COLLATE \"C\" NOT NULL,\n revision bigint NOT NULL, sealed boolean NOT NULL,\n PRIMARY KEY(entity,scope),\n CHECK(octet_length(entity) BETWEEN 1 AND 256),\n CHECK(octet_length(scope) BETWEEN 1 AND 128), CHECK(revision>=0), CHECK(NOT sealed OR revision>0)\n)", "LOCK TABLE stego_resource_state IN SHARE ROW EXCLUSIVE MODE", "INSERT INTO stego_resource_state_scopes(entity,scope,revision,sealed) SELECT entity,scope,count(*),false FROM stego_resource_state GROUP BY entity,scope ON CONFLICT(entity,scope) DO NOTHING", "CREATE OR REPLACE FUNCTION stego_guard_resource_state_scope() RETURNS trigger LANGUAGE plpgsql SET search_path=pg_catalog AS $guard$\nBEGIN\n IF TG_OP = 'DELETE' THEN\n  RAISE EXCEPTION 'resource state scope history cannot be removed' USING ERRCODE='23514';\n END IF;\n IF NEW.entity IS DISTINCT FROM OLD.entity OR NEW.scope IS DISTINCT FROM OLD.scope\n    OR OLD.sealed OR OLD.revision=9223372036854775807 OR NEW.revision IS DISTINCT FROM OLD.revision+1 THEN\n  RAISE EXCEPTION 'resource state scope is closed or its revision differs' USING ERRCODE='23514';\n END IF;\n RETURN NEW;\nEND;\n$guard$", "DROP TRIGGER IF EXISTS stego_resource_state_scope_guard ON stego_resource_state_scopes", "CREATE TRIGGER stego_resource_state_scope_guard BEFORE UPDATE OR DELETE ON stego_resource_state_scopes FOR EACH ROW EXECUTE FUNCTION stego_guard_resource_state_scope()", "CREATE OR REPLACE FUNCTION stego_track_resource_state_keys() RETURNS trigger LANGUAGE plpgsql SET search_path=pg_catalog AS $guard$\nDECLARE current_revision bigint; current_sealed boolean; existing_key boolean;\nBEGIN\n EXECUTE format('INSERT INTO %I.stego_resource_state_scopes(entity,scope,revision,sealed) VALUES($1,$2,0,false) ON CONFLICT(entity,scope) DO NOTHING',TG_TABLE_SCHEMA)\n  USING NEW.entity,NEW.scope;\n EXECUTE format('SELECT revision,sealed FROM %I.stego_resource_state_scopes WHERE entity=$1 AND scope=$2 FOR UPDATE',TG_TABLE_SCHEMA)\n  INTO STRICT current_revision,current_sealed USING NEW.entity,NEW.scope;\n EXECUTE format('SELECT EXISTS(SELECT 1 FROM %I.stego_resource_state WHERE entity=$1 AND resource_id=$2 AND scope=$3)',TG_TABLE_SCHEMA)\n  INTO existing_key USING NEW.entity,NEW.resource_id,NEW.scope;\n IF existing_key THEN RETURN NEW; END IF;\n IF current_sealed THEN\n  RAISE EXCEPTION 'resource state scope is closed' USING ERRCODE='23514',CONSTRAINT='stego_resource_state_scope_closed';\n END IF;\n IF current_revision=9223372036854775807 THEN\n  RAISE EXCEPTION 'resource state scope revision is exhausted' USING ERRCODE='23514';\n END IF;\n EXECUTE format('UPDATE %I.stego_resource_state_scopes SET revision=revision+1 WHERE entity=$1 AND scope=$2',TG_TABLE_SCHEMA)\n  USING NEW.entity,NEW.scope;\n RETURN NEW;\nEND;\n$guard$", "DROP TRIGGER IF EXISTS stego_resource_state_key_guard ON stego_resource_state", "CREATE TRIGGER stego_resource_state_key_guard BEFORE INSERT ON stego_resource_state FOR EACH ROW EXECUTE FUNCTION stego_track_resource_state_keys()"} {
				if err := tx.Exec(statement).Error; err != nil {
					return err
				}
			}
			return nil
		})
	})
}

func verifyResourceStateScopes(db *gorm.DB) error {
	parent := db.Statement.Context
	if parent == nil {
		parent = context.Background()
	}
	ctx, cancel := context.WithTimeout(parent, transactionTimeout)
	defer cancel()
	var valid bool
	err := db.WithContext(ctx).Raw(`SELECT
 (SELECT count(*)=4 FROM pg_catalog.pg_attribute a WHERE a.attrelid=pg_catalog.to_regclass('stego_resource_state_scopes') AND a.attnum>0 AND NOT a.attisdropped AND a.attnotnull
  AND ((a.attname IN ('entity','scope') AND a.atttypid='pg_catalog.text'::regtype AND a.attcollation='pg_catalog."C"'::regcollation)
  OR (a.attname='revision' AND a.atttypid='pg_catalog.int8'::regtype)
  OR (a.attname='sealed' AND a.atttypid='pg_catalog.bool'::regtype)))
 AND EXISTS(SELECT 1 FROM pg_catalog.pg_constraint c JOIN pg_catalog.pg_index i ON i.indexrelid=c.conindid
  WHERE c.conrelid=pg_catalog.to_regclass('stego_resource_state_scopes') AND c.contype='p' AND NOT c.condeferrable AND c.convalidated
  AND i.indisvalid AND i.indisready AND i.indisunique AND i.indnkeyatts=2 AND i.indpred IS NULL AND i.indexprs IS NULL
  AND (SELECT array_agg(a.attname::text ORDER BY k.ordinality) FROM unnest(c.conkey) WITH ORDINALITY k(num,ordinality)
   JOIN pg_catalog.pg_attribute a ON a.attrelid=c.conrelid AND a.attnum=k.num)=ARRAY['entity','scope']::text[])
 AND EXISTS(SELECT 1 FROM pg_catalog.pg_class WHERE oid=pg_catalog.to_regclass('stego_resource_state_scopes') AND relkind='r' AND NOT relrowsecurity)
 AND (SELECT count(DISTINCT pg_catalog.pg_get_expr(c.conbin,c.conrelid))=4 FROM pg_catalog.pg_constraint c
  WHERE c.conrelid=pg_catalog.to_regclass('stego_resource_state_scopes') AND c.contype='c' AND c.convalidated AND pg_catalog.pg_get_expr(c.conbin,c.conrelid) IN (
   '((octet_length(entity) >= 1) AND (octet_length(entity) <= 256))',
   '((octet_length(scope) >= 1) AND (octet_length(scope) <= 128))', '(revision >= 0)', '((NOT sealed) OR (revision > 0))'))`).Scan(&valid).Error
	if err != nil {
		return err
	}
	if !valid {
		return errors.New("resource state scope migration is required")
	}
	for _, guard := range []struct {
		Table, Trigger, Function, Body string
		Kind                           int
	}{
		{"stego_resource_state_scopes", "stego_resource_state_scope_guard", "stego_guard_resource_state_scope", "\nBEGIN\n IF TG_OP = 'DELETE' THEN\n  RAISE EXCEPTION 'resource state scope history cannot be removed' USING ERRCODE='23514';\n END IF;\n IF NEW.entity IS DISTINCT FROM OLD.entity OR NEW.scope IS DISTINCT FROM OLD.scope\n    OR OLD.sealed OR OLD.revision=9223372036854775807 OR NEW.revision IS DISTINCT FROM OLD.revision+1 THEN\n  RAISE EXCEPTION 'resource state scope is closed or its revision differs' USING ERRCODE='23514';\n END IF;\n RETURN NEW;\nEND;\n", 27},
		{"stego_resource_state", "stego_resource_state_key_guard", "stego_track_resource_state_keys", "\nDECLARE current_revision bigint; current_sealed boolean; existing_key boolean;\nBEGIN\n EXECUTE format('INSERT INTO %I.stego_resource_state_scopes(entity,scope,revision,sealed) VALUES($1,$2,0,false) ON CONFLICT(entity,scope) DO NOTHING',TG_TABLE_SCHEMA)\n  USING NEW.entity,NEW.scope;\n EXECUTE format('SELECT revision,sealed FROM %I.stego_resource_state_scopes WHERE entity=$1 AND scope=$2 FOR UPDATE',TG_TABLE_SCHEMA)\n  INTO STRICT current_revision,current_sealed USING NEW.entity,NEW.scope;\n EXECUTE format('SELECT EXISTS(SELECT 1 FROM %I.stego_resource_state WHERE entity=$1 AND resource_id=$2 AND scope=$3)',TG_TABLE_SCHEMA)\n  INTO existing_key USING NEW.entity,NEW.resource_id,NEW.scope;\n IF existing_key THEN RETURN NEW; END IF;\n IF current_sealed THEN\n  RAISE EXCEPTION 'resource state scope is closed' USING ERRCODE='23514',CONSTRAINT='stego_resource_state_scope_closed';\n END IF;\n IF current_revision=9223372036854775807 THEN\n  RAISE EXCEPTION 'resource state scope revision is exhausted' USING ERRCODE='23514';\n END IF;\n EXECUTE format('UPDATE %I.stego_resource_state_scopes SET revision=revision+1 WHERE entity=$1 AND scope=$2',TG_TABLE_SCHEMA)\n  USING NEW.entity,NEW.scope;\n RETURN NEW;\nEND;\n", 7},
	} {
		err = db.WithContext(ctx).Raw(`SELECT EXISTS(SELECT 1 FROM pg_catalog.pg_trigger t JOIN pg_catalog.pg_proc p ON p.oid=t.tgfoid JOIN pg_catalog.pg_language l ON l.oid=p.prolang
   WHERE t.tgrelid=pg_catalog.to_regclass(?) AND t.tgname=? AND t.tgtype=? AND t.tgenabled IN ('O','A')
   AND t.tgqual IS NULL AND t.tgnargs=0 AND NOT t.tgisinternal AND t.tgattr=''::pg_catalog.int2vector
   AND p.proname=? AND p.prosrc=? AND NOT p.prosecdef AND l.lanname='plpgsql'
   AND p.proconfig=ARRAY['search_path=pg_catalog']::text[])`, guard.Table, guard.Trigger, guard.Kind, guard.Function, guard.Body).Scan(&valid).Error
		if err != nil {
			return err
		}
		if !valid {
			return errors.New("resource state scope guard is required")
		}
	}
	return nil
}

func (s *Store) LoadResourceStateScope(ctx context.Context, entity, scope string) (statecontract.ResourceStateScope, error) {
	empty := statecontract.ResourceStateScope{}
	if s == nil || s.db == nil || ctx == nil || !validCheckpointKey(entity, "key", scope) {
		return empty, statecontract.ErrResourceState
	}
	if s.transaction != nil {
		s.transaction.mu.Lock()
		defer s.transaction.mu.Unlock()
		if s.transaction.closed {
			return empty, ErrTransactionClosed
		}
	}
	if err := ctx.Err(); err != nil {
		return empty, err
	}
	operation, cancel := context.WithTimeout(ctx, transactionTimeout)
	defer cancel()
	var rows []statecontract.ResourceStateScope
	if err := s.db.WithContext(operation).Raw("SELECT revision,sealed FROM stego_resource_state_scopes WHERE entity=? AND scope=?", entity, scope).Scan(&rows).Error; err != nil {
		return empty, err
	}
	if len(rows) == 0 {
		return empty, nil
	}
	if len(rows) != 1 || rows[0].Revision < 0 || rows[0].Sealed && rows[0].Revision == 0 {
		return empty, statecontract.ErrResourceState
	}
	return rows[0], nil
}

// SealResourceStateScope shares the caller's transaction. A failed seal prevents
// commit even when the caller ignores its error. New-key inserts use this same
// row lock in the database trigger. Existing record updates remain independent.
func (s *Store) SealResourceStateScope(ctx context.Context, entity, scope string, expected int64) (result statecontract.ResourceStateScope, failure error) {
	if s == nil || s.transaction == nil {
		return result, ErrTransactionRequired
	}
	state := s.transaction
	state.mu.Lock()
	defer state.mu.Unlock()
	if state.closed {
		return result, ErrTransactionClosed
	}
	if state.failure != nil {
		return result, state.failure
	}
	defer func() {
		if failure != nil {
			state.failure = failure
		}
	}()
	if ctx == nil || !validCheckpointKey(entity, "key", scope) || expected < 0 {
		return result, statecontract.ErrResourceState
	}
	if err := ctx.Err(); err != nil {
		return result, err
	}
	operation, cancel := context.WithTimeout(ctx, transactionTimeout)
	defer cancel()
	if err := s.db.WithContext(operation).Exec("INSERT INTO stego_resource_state_scopes(entity,scope,revision,sealed) VALUES(?,?,0,false) ON CONFLICT(entity,scope) DO NOTHING", entity, scope).Error; err != nil {
		return result, err
	}
	var rows []statecontract.ResourceStateScope
	if err := s.db.WithContext(operation).Raw("SELECT revision,sealed FROM stego_resource_state_scopes WHERE entity=? AND scope=? FOR UPDATE", entity, scope).Scan(&rows).Error; err != nil {
		return result, err
	}
	if len(rows) != 1 || rows[0].Revision < 0 || rows[0].Sealed && rows[0].Revision == 0 {
		return result, statecontract.ErrResourceState
	}
	result = rows[0]
	if result.Revision != expected {
		return statecontract.ResourceStateScope{}, statecontract.ErrResourceStateConflict
	}
	if result.Sealed {
		return result, nil
	}
	if expected == math.MaxInt64 {
		return statecontract.ResourceStateScope{}, statecontract.ErrResourceState
	}
	write := s.db.WithContext(operation).Exec("UPDATE stego_resource_state_scopes SET revision=revision+1,sealed=true WHERE entity=? AND scope=? AND revision=? AND NOT sealed", entity, scope, expected)
	if write.Error != nil {
		return statecontract.ResourceStateScope{}, write.Error
	}
	if write.RowsAffected != 1 {
		return statecontract.ResourceStateScope{}, statecontract.ErrResourceStateConflict
	}
	return statecontract.ResourceStateScope{Revision: expected + 1, Sealed: true}, nil
}
