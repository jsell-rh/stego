kind: component
name: postgres-adapter
version: 3.13.1
output_namespace: internal/storage

config:
  migrations:
    type: string
    default: startup
    enum: [startup, external]
  connection_string:
    type: string

requires: []

provides:
  - storage-adapter

slots: []
