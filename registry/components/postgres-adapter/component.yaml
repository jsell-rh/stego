kind: component
name: postgres-adapter
version: 4.13.0
output_namespace: internal/storage

config:
  schema_generation:
    type: string
  writer_lease:
    type: string
    enum: [single, off]
  migrations:
    type: string
    default: startup
    enum: [startup, external]
  connection_string:
    type: string

requires: []

provides:
  - storage-adapter

slots: []
