kind: component
name: postgres-adapter
version: 4.8.0
output_namespace: internal/storage

config:
  schema_generation:
    type: string
  migrations:
    type: string
    default: startup
    enum: [startup, external]
  connection_string:
    type: string

requires: []

provides:
  - storage-adapter

slots: []
