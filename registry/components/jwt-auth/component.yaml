kind: component
name: jwt-auth
version: 2.1.0
output_namespace: internal/auth

config:
  mode:
    type: string
    default: middleware
    enum: [middleware, verifier]
  header:
    type: string
    default: Authorization
  issuer:
    type: string
  audience:
    type: string
  public_key_file:
    type: string
  roles_claim:
    type: string

requires: []

provides:
  - auth-provider

slots: []
