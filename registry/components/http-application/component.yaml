kind: component
name: http-application
version: 1.4.2
output_namespace: application
config:
  factory_package:
    type: string
requires:
  - storage-adapter
  - auth-provider
provides:
  - application-http
slots: []
