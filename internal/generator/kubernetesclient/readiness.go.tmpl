// Code generated by stego. DO NOT EDIT.
package {{.Package}}

import (
 "encoding/json"
 "errors"
)

var ErrResourceObservation = errors.New("Kubernetes resource observation is invalid")

// DeploymentAvailable requires an owned, active Deployment whose current
// generation has the requested available replicas and no incomplete rollout.
// This is a Kubernetes observation, not proof of application or network health.
// A missing status is pending. Malformed status and foreign ownership are errors.
func DeploymentAvailable(current Object, owner Owner, replicas int64) (bool, error) {
 if replicas < 1 || replicas > 10000 || !owner.Matches(current) || !identity(current) ||
  String(current,"apiVersion") != "apps/v1" || String(current,"kind") != "Deployment" ||
  String(current,"metadata","name") == "" || String(current,"metadata","namespace") == "" {
  return false, ErrResourceObservation
 }
 if value := Nested(current,"metadata","deletionTimestamp"); value != nil {
  timestamp, ok := value.(string)
  if !ok { return false, ErrResourceObservation }
  if timestamp != "" { return false, nil }
 }
 if value := Nested(current,"spec","paused"); value != nil {
  paused, ok := value.(bool)
  if !ok { return false, ErrResourceObservation }
  if paused { return false, nil }
 }
 generation, err := observationInteger(Nested(current,"metadata","generation"))
 if err != nil || generation < 1 { return false, ErrResourceObservation }
 desired, err := observationInteger(Nested(current,"spec","replicas"))
 if err != nil { return false, err }
 if desired != replicas { return false, nil }
 status, ok := observationMap(Nested(current,"status"))
 if !ok { return false, ErrResourceObservation }
 observed, err := observationInteger(status["observedGeneration"])
 if err != nil { return false, err }
 if observed != generation { return false, nil }
 for _, field := range []string{"replicas", "updatedReplicas", "readyReplicas", "availableReplicas", "unavailableReplicas"} {
  count, err := observationInteger(status[field])
  if err != nil { return false, err }
  want := replicas
  if field == "unavailableReplicas" { want = 0 }
  if count != want { return false, nil }
 }
 if status["conditions"] == nil { return false, nil }
 conditions, ok := status["conditions"].([]any)
 if !ok || len(conditions) > 64 { return false, ErrResourceObservation }
 seen, available := map[string]bool{}, false
 for _, raw := range conditions {
  condition, ok := observationMap(raw)
  if !ok { return false, ErrResourceObservation }
  kind, k := condition["type"].(string)
  value, v := condition["status"].(string)
  if !k || !v || kind == "" || len(kind) > 128 || seen[kind] || (value != "True" && value != "False" && value != "Unknown") {
   return false, ErrResourceObservation
  }
  seen[kind] = true
  if kind == "Available" { available = value == "True" }
  if (kind == "Progressing" && value != "True") || (kind == "ReplicaFailure" && value != "False") { return false, nil }
 }
 return available, nil
}

func observationMap(value any) (map[string]any, bool) {
 if value == nil { return map[string]any{}, true }
 switch value := value.(type) {
 case Object: return map[string]any(value), true
 case map[string]any: return value, true
 default: return nil, false
 }
}

// API decoding preserves JSON integers. Floats and numeric strings are refused.
func observationInteger(value any) (int64, error) {
 if value == nil { return 0, nil }
 number, ok := value.(json.Number)
 if !ok { return 0, ErrResourceObservation }
 count, err := number.Int64()
 if err != nil || count < 0 { return 0, ErrResourceObservation }
 return count, nil
}
