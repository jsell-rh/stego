// Code generated by stego. DO NOT EDIT.
package {{.Package}}
import (
 "bytes"
 "context"
 "encoding/json"
 "errors"
 "fmt"
 "io"
 "net/http"
 "strings"
 transport {{printf "%q" .Transport}}
)
type Object map[string]any
type Options struct{ServerURL,CAFile,TokenFile string}
type Client struct{client *transport.Client;tokenFile string}
func New(o Options)(*Client,error){
 if _,err:=readToken(o.TokenFile);err!=nil{return nil,err}
 c,err:=transport.New(transport.Options{BaseURL:o.ServerURL,CAFile:o.CAFile});if err!=nil{return nil,err}
 return &Client{c,o.TokenFile},nil
}
func(c *Client)Close(){c.client.Close()}
func readToken(file string) (string, error) {
	value, err := transport.ReadPrivateFile(file)
	if err != nil {
		return "", err
	}
	token := strings.TrimSpace(string(value))
	if token == "" || strings.ContainsAny(token, " \t\r\n") {
		return "", errors.New("invalid Kubernetes token")
	}
	return token, nil
}
func (k *Client) Request(ctx context.Context, method, path string, input Object) (Object, int, error) {
	token, err := readToken(k.tokenFile)
	if err != nil {
		return nil, 0, err
	}
	var body []byte
	if input != nil {
		body, err = json.Marshal(input)
		if err != nil {
			return nil, 0, err
		}
	}
	headers := http.Header{"Authorization": {"Bearer " + token}, "Content-Type": {"application/json"}, "Accept": {"application/json"}}
	if method == http.MethodPatch {
		headers.Set("Content-Type", "application/merge-patch+json")
	}
	response, err := k.client.Do(ctx, method, path, headers, body)
	if err != nil {
		return nil, 0, err
	}
	if response.StatusCode == 404 && (method == http.MethodGet || method == http.MethodDelete) {
		return nil, 404, nil
	}
	if response.StatusCode < 200 || response.StatusCode >= 300 {
		return nil, response.StatusCode, fmt.Errorf("Kubernetes %s failed with status %d", method, response.StatusCode)
	}
	result, err := decodeObject(response.Body)
	if err != nil {
		return nil, response.StatusCode, errors.New("invalid Kubernetes object")
	}
	return result, response.StatusCode, nil
}
func Nested(o Object, keys ...string) any {
	var value any = map[string]any(o)
	for _, key := range keys {
		m, ok := value.(map[string]any)
		if !ok {
			if typed, yes := value.(Object); yes {
				m = typed
			} else {
				return nil
			}
		}
		value = m[key]
	}
	return value
}
func String(o Object, keys ...string) string { v, _ := Nested(o, keys...).(string); return v }

// Owner supplies immutable identity labels selected by the application. The
// client requires every label on desired and existing resources. It never
// adopts resources with missing or different labels.
type Owner map[string]string
func(o Owner)valid()bool{
 if len(o)==0 || len(o)>8{return false}
 for key,value:=range o{if key=="" || len(key)>253 || value=="" || len(value)>63{return false}}
 return true
}
func(o Owner)Matches(object Object)bool{
 if !o.valid(){return false}
 for key,value:=range o{if String(object,"metadata","labels",key)!=value{return false}}
 return true
}
// JSON numbers retain their exact text. Integer fields must not pass through
// float64, which loses precision for valid Kubernetes int64 values.
func decodeObject(body []byte)(Object,error){
 decoder:=json.NewDecoder(bytes.NewReader(body));decoder.UseNumber()
 var result Object
 if err:=decoder.Decode(&result);err!=nil || result==nil{return nil,errors.New("invalid Kubernetes object")}
 var extra any
 if decoder.Decode(&extra)!=io.EOF{return nil,errors.New("invalid Kubernetes object")}
 return result,nil
}
func snapshot(value Object)(Object,error){
 body,err:=json.Marshal(value);if err!=nil{return nil,errors.New("invalid Kubernetes desired object")}
 if len(body)>transport.MaxRequestBytes{return nil,errors.New("Kubernetes desired object exceeds its limit")}
 return decodeObject(body)
}
// Contains ignores extra API defaults and status. Invalid desired JSON is never
// considered a match. Array order and length remain significant.
func Contains(actual,desired Object)bool{
 normalized,err:=snapshot(desired);if err!=nil{return false}
 observed,err:=snapshot(actual);return err==nil && subset(observed,normalized)
}
func subset(actual,desired any)bool{
 if typed,ok:=actual.(Object);ok{actual=map[string]any(typed)}
 if typed,ok:=desired.(Object);ok{desired=map[string]any(typed)}
 switch d:=desired.(type){
 case map[string]any:
  a,ok:=actual.(map[string]any);if !ok{return false}
  for key,value:=range d{if !subset(a[key],value){return false}};return true
 case []any:
  a,ok:=actual.([]any);if !ok || len(a)!=len(d){return false}
  for i:=range d{if !subset(a[i],d[i]){return false}};return true
 default:return actual==desired
 }
}
func identity(object Object)bool{return String(object,"metadata","uid")!="" && String(object,"metadata","resourceVersion")!=""}

// Ensure creates an absent object or applies a merge patch to an owned object.
// The observed UID and version guard each patch. The caller retains ownership
// of desired; the client never changes it. Conflicts require a new observation.
func(c *Client)Ensure(ctx context.Context,collection string,desired Object,owner Owner)(Object,error){
 want,err:=snapshot(desired);if err!=nil{return nil,err}
 name:=String(want,"metadata","name")
 if name=="" || strings.ContainsAny(name,"/\\?#% \t\r\n") || name=="." || name==".." || !owner.Matches(want){return nil,errors.New("Kubernetes desired object requires a name and owner labels")}
 for _,field:=range []string{"uid","resourceVersion","deletionTimestamp"}{
  if Nested(want,"metadata",field)!=nil{return nil,errors.New("Kubernetes desired object contains server-owned metadata")}
 }
 path:=collection+"/"+name
 old,code,err:=c.Request(ctx,http.MethodGet,path,nil);if err!=nil{return nil,err}
 if code==404{created,_,err:=c.Request(ctx,http.MethodPost,collection,want);return created,err}
 if !owner.Matches(old) || !identity(old) || String(old,"metadata","deletionTimestamp")!=""{return nil,errors.New("Kubernetes resource has a different owner, has no identity, or is being deleted")}
 if subset(old,want){return old,nil}
 metadata:=want["metadata"].(map[string]any)
 metadata["resourceVersion"]=String(old,"metadata","resourceVersion")
 metadata["uid"]=String(old,"metadata","uid")
 updated,_,err:=c.Request(ctx,http.MethodPatch,path,want);return updated,err
}

// DeleteOwned returns true only when the resource is absent. A submitted or
// pending deletion returns false. The caller must observe it again. No delete
// follows a missing identity or failed ownership check.
func(c *Client)DeleteOwned(ctx context.Context,path string,owner Owner)(bool,error){
 if !owner.valid(){return false,errors.New("Kubernetes deletion requires owner labels")}
 old,code,err:=c.Request(ctx,http.MethodGet,path,nil);if err!=nil{return false,err};if code==404{return true,nil}
 if !owner.Matches(old) || !identity(old){return false,errors.New("Kubernetes resource has a different owner or has no identity")}
 if String(old,"metadata","deletionTimestamp")!=""{return false,nil}
 _,code,err=c.Request(ctx,http.MethodDelete,path,Object{"apiVersion":"v1","kind":"DeleteOptions","preconditions":Object{"uid":String(old,"metadata","uid"),"resourceVersion":String(old,"metadata","resourceVersion")},"propagationPolicy":"Foreground"})
 return code==404,err
}
