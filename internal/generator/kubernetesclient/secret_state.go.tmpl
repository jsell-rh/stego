// Code generated by stego. DO NOT EDIT.
package {{.Package}}

import (
 "context"
 "encoding/hex"
 "errors"
 "net/http"
 "time"
)

var ErrSecretState = errors.New("durable Secret state is missing or differs from its retained identity")
var ErrSecretStatePending = errors.New("durable Secret state awaits its namespace identity")

// SecretStateBinding is stored outside the namespace. A closed binding prevents
// new use but permits reads for cleanup. A missing binding is not a deletion.
type SecretStateBinding struct { Present,Closed bool; Digest string }
// SecretStateOptions selects one immutable Secret and one public digest marker.
// The namespace allocator must copy the marker's sha256 field to Annotation.
type SecretStateOptions struct { Namespace,Name,Marker,Annotation string; Owner Owner }
// SecretStateCallbacks keep application policy outside the Kubernetes runtime.
// Initialize must prove that no prior external state exists before it returns
// new data. Validate checks application data and returns its stable digest.
// Bind must conditionally register that digest against current live resource
// state and refuse registration after deletion. Callbacks must honor context.
type SecretStateCallbacks struct {
 Load func(context.Context)(SecretStateBinding,error)
 Bind func(context.Context,string)(SecretStateBinding,error)
 Initialize func(context.Context)(map[string]string,error)
 Validate func(Object)(string,error)
}
func secretStateDigest(value string)bool {raw,err:=hex.DecodeString(value);return err==nil&&len(raw)==32&&hex.EncodeToString(raw)==value}
func secretStateBindingValid(value SecretStateBinding)bool {
 if !value.Present{return !value.Closed&&value.Digest==""}
 return (value.Closed&&value.Digest=="")||secretStateDigest(value.Digest)
}
func secretStateActive(value Object,owner Owner,kind,namespace,name string)bool {
 if !owner.Matches(value)||!identity(value)||String(value,"apiVersion")!="v1"||String(value,"kind")!=kind||String(value,"metadata","name")!=name||String(value,"metadata","namespace")!=namespace{return false}
 deletion:=Nested(value,"metadata","deletionTimestamp");return deletion==nil||deletion==""
}

// LoadSecretState recovers an immutable, registered Secret. With create=true,
// it can create initial state and its public marker, but never replace either
// record. It returns no Secret until the namespace pin and external binding
// match. It does not write namespace annotations or perform external effects.
func(c *Client)LoadSecretState(ctx context.Context,o SecretStateOptions,cb SecretStateCallbacks,create bool)(Object,error){
 if ctx==nil||!routeLabel(o.Namespace)||!routeDNS(o.Name)||!routeDNS(o.Marker)||o.Annotation==""||len(o.Annotation)>253||!o.Owner.valid()||cb.Load==nil||cb.Validate==nil||(create&&(cb.Initialize==nil||cb.Bind==nil)){return nil,ErrSecretState}
 ctx,cancel:=context.WithTimeout(ctx,20*time.Second);defer cancel()
 binding,err:=cb.Load(ctx);if err!=nil{return nil,err}
 if !secretStateBindingValid(binding)||(create&&binding.Closed){return nil,ErrSecretState}
 namespace,code,err:=c.Request(ctx,http.MethodGet,"/api/v1/namespaces/"+o.Namespace,nil);if err!=nil{return nil,err}
 if code==404{return nil,ErrSecretStatePending}
 if code!=200||!secretStateActive(namespace,o.Owner,"Namespace","",o.Namespace){return nil,ErrSecretState}
 rawPin:=Nested(namespace,"metadata","annotations",o.Annotation)
 pinned:="";if rawPin!=nil{var ok bool;pinned,ok=rawPin.(string);if !ok||(pinned!=""&&!secretStateDigest(pinned)){return nil,ErrSecretState}}
 core:="/api/v1/namespaces/"+o.Namespace
 marker,markerCode,err:=c.Request(ctx,http.MethodGet,core+"/configmaps/"+o.Marker,nil);if err!=nil{return nil,err}
 if markerCode!=404&&(markerCode!=200||!secretStateActive(marker,o.Owner,"ConfigMap",o.Namespace,o.Marker)||marker["immutable"]!=true){return nil,ErrSecretState}
 secret,code,err:=c.Request(ctx,http.MethodGet,core+"/secrets/"+o.Name,nil);if err!=nil{return nil,err}
 definition:=func(kind,name string)Object{return Object{"apiVersion":"v1","kind":kind,"metadata":Object{"name":name,"namespace":o.Namespace,"labels":o.Owner},"immutable":true}}
 validate:=func(secret Object)(string,error){
  if !secretStateActive(secret,o.Owner,"Secret",o.Namespace,o.Name)||secret["immutable"]!=true{return "",ErrSecretState}
  if _,err:=OpaqueSecretSetDigest(o.Namespace,[]string{o.Name},o.Owner,[]Object{secret});err!=nil{return "",ErrSecretState}
  digest,err:=cb.Validate(secret);if err!=nil{return "",err};if !secretStateDigest(digest){return "",ErrSecretState};return digest,nil
 }
 if code==404 {
  if !create||binding.Present||pinned!=""||markerCode!=404{return nil,ErrSecretState}
  values,err:=cb.Initialize(ctx);if err!=nil{return nil,err}
  secret=definition("Secret",o.Name);secret["type"]="Opaque"
  data:=Object{};for key,value:=range values{data[key]=value};secret["data"]=data
  if _,_,err=opaqueSecretData(data);err!=nil{return nil,ErrSecretState}
  prepared,err:=cb.Validate(secret);if err!=nil{return nil,err};if !secretStateDigest(prepared){return nil,ErrSecretState}
  secret,code,err=c.Request(ctx,http.MethodPost,core+"/secrets",secret);if err!=nil{return nil,err}
 }
 if code!=200&&code!=201{return nil,ErrSecretState}
 digest,err:=validate(secret);if err!=nil{return nil,err}
 if (binding.Present&&binding.Digest!=digest)||(pinned!=""&&pinned!=digest){return nil,ErrSecretState}
 if markerCode==404 {
  if !create{return nil,ErrSecretState}
  marker=definition("ConfigMap",o.Marker);marker["data"]=Object{"sha256":digest}
  marker,markerCode,err=c.Request(ctx,http.MethodPost,core+"/configmaps",marker);if err!=nil{return nil,err}
 }
 if (markerCode!=200&&markerCode!=201)||!secretStateActive(marker,o.Owner,"ConfigMap",o.Namespace,o.Marker)||marker["immutable"]!=true||String(marker,"data","sha256")!=digest{return nil,ErrSecretState}
 if pinned!=digest{return nil,ErrSecretStatePending}
 // Recheck namespace identity before the external registration. A replacement
 // with the same name must not use this observation's retained keys.
 current,code,err:=c.Request(ctx,http.MethodGet,"/api/v1/namespaces/"+o.Namespace,nil);if err!=nil{return nil,err}
 if code!=200||!secretStateActive(current,o.Owner,"Namespace","",o.Namespace)||String(current,"metadata","uid")!=String(namespace,"metadata","uid")||String(current,"metadata","annotations",o.Annotation)!=digest{return nil,ErrSecretState}
 if create {
  binding,err=cb.Bind(ctx,digest);if err!=nil{return nil,err}
  if !secretStateBindingValid(binding)||!binding.Present||binding.Closed||binding.Digest!=digest{return nil,ErrSecretState}
 }else if !binding.Present||binding.Digest!=digest{return nil,ErrSecretState}
 if err:=ctx.Err();err!=nil{return nil,err}
 return secret,nil
}
