// Code generated by stego. DO NOT EDIT.
package {{.Package}}

import (
 "bytes"
 "context"
 "errors"
 "io"
 "net/http"
 "net/url"
 "strings"
 "strconv"
 "sync"
 "time"
 wire {{printf "%q" .Wire}}
 transport {{printf "%q" .Transport}}
 {{if .Tracing}}tracing {{printf "%q" .Tracing}}{{end}}
)

type Options struct { BaseURL,Token,CAFile string; Timeout time.Duration }
// Client owns a bounded HTTPS transport. Close releases its resources.
type Client struct {
 wire *wire.ClientWithResponses
 transport *transport.Client
 timeout time.Duration
 permits chan struct{}
 mu sync.Mutex
 closed bool
 calls int
 drained chan struct{}
 once sync.Once
 {{if .Tracing}}runtime *tracing.Runtime{{end}}
}
func NewClient(options Options)(*Client,error){
 if len(options.Token)==0||len(options.Token)>16384{return nil,errors.New("SDK bearer token is required")}
 for _,r:=range options.Token{if !((r>='a'&&r<='z')||(r>='A'&&r<='Z')||(r>='0'&&r<='9')||strings.ContainsRune("-._~+/=",r)){return nil,errors.New("invalid SDK bearer token")}}
 base,err:=url.Parse(options.BaseURL)
 if err!=nil{return nil,errors.New("invalid SDK base URL")}
 shared,err:=transport.New(transport.Options{BaseURL:options.BaseURL,CAFile:options.CAFile,Timeout:options.Timeout})
 if err!=nil{return nil,err}
 timeout:=options.Timeout;if timeout==0{timeout=transport.RequestTimeout}
 c:=&Client{transport:shared,timeout:timeout,permits:make(chan struct{},transport.MaxConcurrentRequests),drained:make(chan struct{})}
 {{if .Tracing}}
 c.runtime,err=tracing.NewRuntime()
 if err!=nil{shared.Close();return nil,errors.New("cannot initialize SDK telemetry")}
 {{end}}
 c.wire,err=wire.NewClientWithResponses(strings.TrimRight(options.BaseURL,"/"),wire.WithHTTPClient(&sdkTransport{client:shared,base:base,token:options.Token}))
 if err!=nil{c.Close();return nil,errors.New("cannot initialize SDK client")}
 return c,nil
}
func(c *Client)acquire(ctx context.Context)error{
 if c==nil||ctx==nil{return errors.New("SDK client is unavailable")}
 if err:=ctx.Err();err!=nil{return err}
 c.mu.Lock();defer c.mu.Unlock()
 if c.closed{return errors.New("SDK client is closed")}
 select{case c.permits<-struct{}{}:c.calls++;return nil;default:return errors.New("SDK client capacity exceeded")}
}
func(c *Client)release(){
 <-c.permits
 c.mu.Lock();defer c.mu.Unlock();c.calls--;if c.closed&&c.calls==0{close(c.drained)}
}
func(c *Client)bind(ctx *context.Context){ {{if .Tracing}}*ctx=c.runtime.Context(*ctx){{end}} }
func(c *Client)Close(){
 if c==nil{return}
 c.once.Do(func(){
  c.mu.Lock();c.closed=true;if c.calls==0{close(c.drained)};c.mu.Unlock()
  c.transport.Close()
  // Caller-defined JSON methods cannot be forcibly stopped. Bound the wait.
  timer:=time.NewTimer(time.Second);defer timer.Stop()
  select{case <-c.drained:case <-timer.C:}
  {{if .Tracing}}c.runtime.Close(){{end}}
 })
}
// Only the generated wrapper can construct this bridge. Request editors and
// raw HTTP clients are not part of the public SDK API.
type sdkTransport struct{client *transport.Client;base *url.URL;token string}
func(s *sdkTransport)Do(request *http.Request)(*http.Response,error){
 if request==nil||request.URL==nil||request.URL.Scheme!=s.base.Scheme||request.URL.Host!=s.base.Host||request.URL.User!=nil||request.URL.Fragment!=""{return nil,errors.New("SDK request changed its origin")}
 prefix:=strings.TrimRight(s.base.EscapedPath(),"/")
 escaped:=request.URL.EscapedPath()
 if prefix!=""&&!strings.HasPrefix(escaped,prefix+"/"){return nil,errors.New("SDK request changed its path prefix")}
 relative:=strings.TrimPrefix(escaped,prefix)
 if request.URL.RawQuery!=""{relative+="?"+request.URL.RawQuery}
 var body []byte
 if request.Body!=nil{
  var err error
  body,err=io.ReadAll(io.LimitReader(request.Body,transport.MaxRequestBytes+1));request.Body.Close()
  if err!=nil||len(body)>transport.MaxRequestBytes{return nil,errors.New("invalid SDK request body")}
 }
 headers:=request.Header.Clone();if headers==nil{headers=make(http.Header)}
 headers.Set("Authorization","Bearer "+s.token)
 response,err:=s.client.Do(request.Context(),request.Method,relative,headers,body)
 if err!=nil{return nil,err}
 return &http.Response{Status:strconv.Itoa(response.StatusCode)+" "+http.StatusText(response.StatusCode),StatusCode:response.StatusCode,Header:response.Header,Body:io.NopCloser(bytes.NewReader(response.Body)),ContentLength:int64(len(response.Body)),Request:request},nil
}
