// Code generated by STEGO. DO NOT EDIT.
package {{.Package}}

import (
 "bytes"
 "context"
 "errors"
 "fmt"
 "math"
 "time"
)

var ErrStateJournal = errors.New("protected state journal contract is invalid")

// SealedStateRecord is the storage boundary. Version zero means no record and
// requires empty Data. Other versions require an authenticated envelope.
type SealedStateRecord struct { Version int64; Data []byte }
func(SealedStateRecord)Format(s fmt.State,_ rune){_,_=s.Write([]byte("[sealed state record]"))}
func(SealedStateRecord)MarshalJSON()([]byte,error){return nil,ErrStateJournal}

// StatePersistence is bound to one resource and scope. The caller must enforce
// authorization and the observed resource revision. Save must atomically compare
// the stored version with expected, add one, and return the committed record.
// Keep version history on deletion. Do not retry uncertain writes. Both callbacks
// must stop on cancellation and must not retain their byte arguments.
type StatePersistence struct {
 Load func(context.Context)(SealedStateRecord,error)
 Save func(context.Context,int64,[]byte)(SealedStateRecord,error)
}

// StateJournal adds encryption, response checks, and a ten-second operation
// deadline to application storage. It has no mutable version cache. Concurrent
// saves use the storage compare-and-swap; a failure requires another Load.
type StateJournal struct {
 protector *StateProtector
 key StateKey
 persistence StatePersistence
 limit int
}
func(StateJournal)Format(s fmt.State,_ rune){_,_=s.Write([]byte("[protected state journal]"))}
func(StateJournal)MarshalJSON()([]byte,error){return nil,ErrStateJournal}

// StateSnapshot comes from one journal. Its zero value cannot be saved. Plaintext
// requires explicit Reveal. A new journal after restart must load a new snapshot.
type StateSnapshot struct { journal *StateJournal; version int64; state ProtectedState }
func(s StateSnapshot)Version()int64{return s.version}
func(s StateSnapshot)Reveal()[]byte{return s.state.Reveal()}
func(StateSnapshot)Format(s fmt.State,_ rune){_,_=s.Write([]byte("[protected state snapshot]"))}
func(StateSnapshot)MarshalJSON()([]byte,error){return nil,ErrStateJournal}

func NewStateJournal(protector *StateProtector,key StateKey,persistence StatePersistence,maxSealedBytes int)(*StateJournal,error){
 if protector==nil||len(protector.keys)==0||persistence.Load==nil||persistence.Save==nil||maxSealedBytes<stateHeaderSize+28||maxSealedBytes>MaxProtectedStateBytes{return nil,ErrStateJournal}
 if _,err:=stateAdditionalData(key,1);err!=nil{return nil,err}
 return &StateJournal{protector:protector,key:key,persistence:persistence,limit:maxSealedBytes},nil
}

func(j *StateJournal)Load(ctx context.Context)(StateSnapshot,error){
 if j==nil||j.protector==nil||j.persistence.Load==nil||ctx==nil{return StateSnapshot{},ErrStateJournal}
 if err:=ctx.Err();err!=nil{return StateSnapshot{},err}
 operation,cancel:=context.WithTimeout(ctx,10*time.Second);defer cancel()
 record,err:=j.persistence.Load(operation);if err!=nil{return StateSnapshot{},err}
 if err=operation.Err();err!=nil{return StateSnapshot{},err}
 if record.Version<0||len(record.Data)>j.limit{return StateSnapshot{},ErrStateJournal}
 if record.Version==0{
  if len(record.Data)!=0{return StateSnapshot{},ErrStateJournal}
  return StateSnapshot{journal:j},nil
 }
 state,err:=j.protector.Open(j.key,record.Version,record.Data);if err!=nil{return StateSnapshot{},err}
 return StateSnapshot{journal:j,version:record.Version,state:state},nil
}

// Save seals the next version before one storage call. A response must contain
// that exact version and envelope. An error can mean the write committed but its
// result was lost. Load again before any later provider effect or state write.
func(j *StateJournal)Save(ctx context.Context,prior StateSnapshot,plain []byte)(StateSnapshot,error){
 if j==nil||j.protector==nil||j.persistence.Save==nil||ctx==nil||prior.journal!=j||prior.version<0||prior.version==math.MaxInt64||len(plain)>j.limit-stateHeaderSize-28{return StateSnapshot{},ErrStateJournal}
 if err:=ctx.Err();err!=nil{return StateSnapshot{},err}
 sealed,err:=j.protector.Seal(j.key,prior.version+1,plain);if err!=nil{return StateSnapshot{},err}
 operation,cancel:=context.WithTimeout(ctx,10*time.Second);defer cancel()
 record,err:=j.persistence.Save(operation,prior.version,bytes.Clone(sealed));if err!=nil{return StateSnapshot{},err}
 if err=operation.Err();err!=nil{return StateSnapshot{},err}
 if record.Version!=prior.version+1||!bytes.Equal(record.Data,sealed){return StateSnapshot{},ErrStateJournal}
 state,err:=j.protector.Open(j.key,record.Version,sealed);if err!=nil{return StateSnapshot{},err}
 return StateSnapshot{journal:j,version:record.Version,state:state},nil
}
